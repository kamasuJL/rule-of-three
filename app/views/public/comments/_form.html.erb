<%= form_with(model:[post, comment], local: false) do |f| %>
  <div class="d-flex flex-column md-6">
    <%= f.text_field :comment, placeholder: "参考になりました！", class: "comment-textarea" %>
  </div>
  <div>
    <%= f.submit "コメントを送信する", class: 'btn btn-secondary mt-2' %>
  </div>
<% end %>
